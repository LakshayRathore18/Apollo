import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, // disable the "development" indicator in the top right corner during development
};

export default nextConfig;
